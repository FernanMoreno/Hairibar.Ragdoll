## 5. Notas abiertas

Decisiones pendientes antes de implementar cambios de producto:

- Cadena de piernas para swing: solo pantorrilla+pie o incluir muslo.
- Política `Ignore`/`Notify` para prop drift.
- Captura RagdollLab de MP5Prop empuñada bajo carga.
- Retune de `minimumGetUpDuration` en prefab de producción.
- Retune y alcance de estabilizador reactivo `SubBehaviourBalancer`.

La nota de nombre está cerrada: `RagdollBipedBalanceBehaviour` fue renombrado a `RagdollBipedStaggerBehaviour` el 2026-08-13.
